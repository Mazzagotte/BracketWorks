// Re-export the shared component so existing imports keep working
export { SortableHeader } from '../../components/SortableHeader';
export type { SortableHeaderProps } from '../../components/SortableHeader';


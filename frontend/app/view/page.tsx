import TournamentDirectory from './TournamentDirectory'

export default function PublicViewDirectoryPage() {
  return (
    <TournamentDirectory
      subtitle="Pick a tournament to open the public live board."
    />
  )
}

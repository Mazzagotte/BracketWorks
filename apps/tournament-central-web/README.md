# Tournament Central

Tournament Central is a lightweight Next.js starter for BracketWorks tournament discovery, creation, and registration flows.

## Development

- Install dependencies from the repository root with `npm install`
- Start the app with `npm --workspace apps/tournament-central run dev`
- Build with `npm --workspace apps/tournament-central run build`
